name: publish site
on: [push]
jobs:
    #prepare:
    #  runs-on: ubuntu-latest
    #  steps:
    #    - name: checkout
    #      uses: actions/checkout@v4
    #    - name: test1
    #      run: ls
    #    - name: test2
    #      run: pwd
    build-site:
      runs-on: ubuntu-latest
      container: clojure:lein
      steps:
        - name: checkout
          uses: actions/checkout@v4
        - name: build
          run: lein build-site